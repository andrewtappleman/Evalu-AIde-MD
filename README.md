# Evalu-AIde-MD
Download the repository
type in cmd the following lines
  cd path\to\your\project
  python -m venv venv
  venv\Scripts\activate
  python -m pip install --upgrade pip
  pip install kivy tensorflow numpy pandas pymongo httpx plyer
  pip install kivy[base] kivy_examples
  pip install docutils pygments pypiwin32
  pip install h5py pillow

There are test imnages to use in the repository
